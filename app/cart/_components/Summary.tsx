import React from "react";
import { Card } from "@/components/ui/card";

type Props = {};

const Summary = (props: Props) => {
  return <Card className="flex h-full w-[30%] p-5"> p5</Card>;
};

export default Summary;
